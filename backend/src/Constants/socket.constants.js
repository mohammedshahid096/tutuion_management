const admin_emit_listeners = {
  adminNotification: "adminNotification",
};

const student_emit_listeners = {
  studentNotification: "studentNotification",
};

const admin_receiver_listeners = {
  join_admin_room: "joinAdminRoom",
};

const student_receiver_listeners = {
  join_student_room: "joinStudentRoom",
};

module.exports = {
  admin_emit_listeners,
  student_emit_listeners,
  admin_receiver_listeners,
  admin_receiver_listeners,
  student_receiver_listeners,
};
