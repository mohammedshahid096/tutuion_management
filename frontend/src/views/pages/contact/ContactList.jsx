import MainWrapper from '@/views/layouts/Mainwrapper';
import React, { memo } from 'react';

const ContactList = () => {
  return <MainWrapper>ContactList</MainWrapper>;
};

export default memo(ContactList);
